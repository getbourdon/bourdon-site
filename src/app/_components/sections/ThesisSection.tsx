export function ThesisSection() {
  return (
    <section className="section" id="thesis">
      <p className="section-lead">
        Every agent-memory framework optimizes for <em>better retrieval.</em> Bourdon optimizes for
        the <em>felt experience</em> of memory — the moment-by-moment shape of the response. The
        difference between &ldquo;the AI knows me&rdquo; and &ldquo;the AI sounds like a database
        that talks.&rdquo;
      </p>
    </section>
  );
}
